{
  "name": "baldu",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web -- -p 3002\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "seed": "npm run seed -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
